# Zero Hour Assault Game Server Configuration
# Feel free to change configuration parameters below

# Server listening port
port = 10000

# SQLite player database (relative to server/ directory)
db_path = zha_players.db

# Admin dashboard
dashboard_port = 8080
dashboard_password = changeme

# Bot API key — any long random string; set the same value in telegram_bot.py
dashboard_api_key = changeme_bot_key

# Telegram bot (leave token blank to disable)
telegram_bot_token =
telegram_admin_ids = 7810113372

# MOTD file path (relative to server/ directory)
motd_file = motd.txt
